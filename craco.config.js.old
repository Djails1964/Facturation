const path = require('path');

module.exports = {
  webpack: {
    alias: {
      '@': path.resolve(__dirname, 'src'),
    },
  },
  devServer: {
    port: 3007,
    host: '127.0.0.1',
    open: true,
    hot: true,
    historyApiFallback: true
  }
};