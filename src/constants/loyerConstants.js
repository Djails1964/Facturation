// src/constants/loyerConstants.js
// ✅ Libellé affiché (tooltip) quand une location ne peut plus être
// modifiée/supprimée/régénérée à cause de la facture qu'elle a générée —
// sans détailler l'état exact de la facture (cf. LocationSalleGestion.jsx).
export const LIBELLES_VERROU_FACTURE_LOCATION = {
    facture: 'Facture non modifiable',
};